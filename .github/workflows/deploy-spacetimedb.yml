name: Deploy SpacetimeDB Module

# Deploys the multiplayer server module to SpacetimeDB Maincloud
# and generates client bindings for the browser.
#
# Triggers on push to main when the spacetimedb/ directory changes.
# Also supports manual trigger for first-time setup.

on:
  push:
    branches: [main]
    paths:
      - 'spacetimedb/**'
      - '.github/workflows/deploy-spacetimedb.yml'
  workflow_dispatch:  # Manual trigger button in GitHub UI

env:
  # Database name on SpacetimeDB Maincloud
  # Change this to your own unique name!
  STDB_DATABASE: void-claim

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Needed to push generated bindings back to repo
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install SpacetimeDB CLI
        run: |
          curl -sSf https://install.spacetimedb.com | sh -s -- -y
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Authenticate with SpacetimeDB
        run: spacetime login --token "${{ secrets.SPACETIMEDB_TOKEN }}"

      - name: Install module dependencies
        run: |
          cd spacetimedb
          npm install

      - name: Publish module to Maincloud
        run: |
          cd spacetimedb
          spacetime publish "${{ env.STDB_DATABASE }}" \
            --server maincloud \
            --delete-data \
            -y \
            2>&1
          echo "✅ Module published to Maincloud"

      - name: Generate TypeScript client bindings
        run: |
          spacetime generate \
            --lang typescript \
            --out-dir ./client-bindings/src \
            --module-path ./spacetimedb
          echo "✅ Client bindings generated"

      - name: Build IIFE bundle for browser
        run: |
          cd client-bindings

          # Create package.json for the bindings bundle
          cat > package.json << 'EOF'
          {
            "name": "void-claim-bindings",
            "private": true,
            "type": "module",
            "scripts": {
              "build": "npx esbuild src/index.ts --bundle --format=iife --global-name=VoidClaimDB --outfile=../dist/bindings.iife.js --platform=browser --target=es2020"
            },
            "dependencies": {
              "spacetimedb": "^2.0.0"
            },
            "devDependencies": {
              "esbuild": "^0.20.0",
              "typescript": "^5.4.0"
            }
          }
          EOF

          npm install
          npm run build
          echo "✅ IIFE bundle built at dist/bindings.iife.js"

      - name: Commit generated bindings
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/ client-bindings/src/
          git diff --cached --quiet && echo "No changes to commit" || \
            (git commit -m "chore: auto-generate SpacetimeDB client bindings" && \
             git push)
          echo "✅ Bindings committed to repo"
